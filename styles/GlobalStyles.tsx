import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
    row: {
        flexDirection: "row",
    },
    title: {
        fontSize: 20,
        fontWeight: 'bold',
        color: 'black'
    }
});